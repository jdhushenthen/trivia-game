const bf = require('baffle');
var canvas = document.getElementById("gameScreen");
var ctx = canvas.getContext('2d');

ctx.textAlign = "center";

canvas.height = window.innerHeight;
canvas.width = window.innerWidth;

